name: Basic CI

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print message
        run: echo "Hello, CI with GitHub Actions!"
